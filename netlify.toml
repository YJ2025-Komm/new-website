[build]
  base = "client"
  command = "npm install && npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/website-analysis"
  to = "/features"
  status = 301
  force = true
